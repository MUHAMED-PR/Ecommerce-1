<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Order Successful</title>

    <!-- Favicon -->
    <link rel="icon" href="/assets/images/icons/favicon.ico">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

    <!-- Custom CSS -->
    <style>
        body {
            background-color: #f7f9fc;
            font-family: 'Montserrat', sans-serif;
            color: #333;
        }

        .order-success-container {
            max-width: 600px;
            margin: 100px auto;
            padding: 40px;
            background-color: #fff;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            text-align: center;
            animation: fadeIn 1s ease;
        }

        .order-success-icon {
            font-size: 4rem;
            color: #28a745;
            margin-bottom: 20px;
        }

        .order-success-title {
            font-size: 2.5rem;
            font-weight: 600;
            color: #2c3e50;
        }

        .order-success-message {
            font-size: 1.2rem;
            margin-top: 10px;
            color: #7f8c8d;
            margin-bottom: 30px;
        }

        .order-success-buttons .btn {
            padding: 12px 30px;
            font-size: 1rem;
            border-radius: 50px;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .btn-primary-custom {
            background-color: #28a745;
            color: white;
            border: none;
        }

        .btn-primary-custom:hover {
            background-color: #218838;
        }

        .btn-outline-custom {
            border-color: #28a745;
            color: #28a745;
        }

        .btn-outline-custom:hover {
            background-color: #28a745;
            color: white;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .footer {
            margin-top: 60px;
            text-align: center;
            color: #7f8c8d;
            font-size: 0.9rem;
        }
    </style>
</head>

<body>

    <!-- Order Success Container -->
    <div class="order-success-container">
        <div class="order-success-icon">
            <i class="fas fa-check-circle"></i>
        </div>
        <h2 class="order-success-title">Order Successful!</h2>
        <p class="order-success-message">Thank you for your purchase! Your order has been placed successfully. We will notify you when it is ready to ship.</p>

        <!-- Buttons -->
        <div class="order-success-buttons">
            <a href="/userProfile?id=123"  class="btn btn-primary-custom">View Orders</a>
            <a href="/" class="btn btn-outline-custom">Continue Shopping</a>
        </div>
    </div>

    <!-- Footer -->
    <div class="footer">
        &copy; 2024 Your Company Name. All rights reserved.
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>



</body>

</html>
